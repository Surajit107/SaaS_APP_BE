# Backend documentation

| Document | Purpose |
|----------|---------|
| [Knowledge transfer guide](./KNOWLEDGE-TRANSFER.md) | What the API does today, how flows work (with diagrams), and what is intentionally out of scope. |

Interactive API reference: run the app and open **Swagger UI** at `/api/docs` (global prefix `api`).
